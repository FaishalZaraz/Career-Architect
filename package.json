{
  "name": "job-tracker-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@job-tracker/web",
    "build": "npm run build --workspace=@job-tracker/web",
    "preview": "npm run preview --workspace=@job-tracker/web"
  }
}
